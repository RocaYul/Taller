using System;
using System.Collections.Generic;
using System.Text;

namespace Taller.Functions.Functions
{
    public class OperactionAPI
    {
        /*string filter = TableQuery.GenerateFilterConditionForInt("IdEmployed", QueryComparisons.LessThan, 2);
            TableQuery<EmployedEntity> query = new TableQuery<EmployedEntity>().Where(filter);
            TableQuerySegment<EmployedEntity> completedTareas = await employedTable.ExecuteQuerySegmentedAsync(query, null);

            string message = "Retrieved all employed";
            log.LogInformation(message);

            return new OkObjectResult(new Response
            {
                IsSuccess = true,
                Message = message,
                Result = completedTareas
            });
        */

    }
}
